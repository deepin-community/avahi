#!/bin/sh

set -e

#DEBHELPER#

if [ "$1" = "purge" ]; then
    deluser --quiet --system avahi-autoipd > /dev/null || true
    delgroup --quiet --system avahi-autoipd > /dev/null || true
    # Cleanup /var/lib/avahi-autoipd, see #527925
    rm -rf /var/lib/avahi-autoipd
fi

